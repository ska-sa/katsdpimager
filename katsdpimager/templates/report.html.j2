<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Spectral imager report</title>
    {{ resources | safe | indent(8) }}
    {{ script | safe | indent(8) }}
  </head>
  <body>
{% for target in targets %}
    <h1>{{ target.name }}</h1>
    <p>{{ target.description }}</p>
    <p>Total channels: {{ common.channels }}</p>
    <p>Computed channels: {{ common.output_channels | length }}</p>
    <p>Successful channels: {{ target.status|select("equalto", "complete")|list|count }}</p>
    <p>Flagged channels: {{ target.status|select("equalto", "no-data")|list|count }}</p>
    {{ target.plots['status'] | safe | indent(4) }}
    {{ target.plots['peak'] | safe | indent(4) }}
{% endfor %}
  </body>
</html>
